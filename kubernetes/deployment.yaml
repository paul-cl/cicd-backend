apiVersion: apps/v1
kind: Deployment
metadata:
  name: 
  labels:
    app: 
spec:
  selector:
    matchLabels:
      app: 
  replicas: 1
  template:
    metadata:
      labels:
        app: 
    spec:
      containers:
        - name: 
          image: 
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
